apiVersion: v1
kind: ConfigMap
metadata:
  name: example-frontend-config
data:
  API_URL: '{{ .Values.protocol }}://{{ .Values.services.example_backend.ingress.subdomain }}.{{ .Values.domain }}'
  AUTH_URL: '{{ .Values.protocol }}://{{ .Values.services.example_backend.ingress.subdomain }}.{{ .Values.domain }}/auth/login'